import * as functions from './arrow-functions'
import {Circle} from './circle'

//let invalidRadiusCircle = new Circle(3.5); // should throw an Error

let myCircle = new Circle(3);
console.log(' Area of myCircle ', myCircle.area());

